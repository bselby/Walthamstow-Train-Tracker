# Realtime Trains API — refresh token from api-portal.rtt.io.
# The Netlify Function exchanges this for a short-lived access token at
# request time (cached in module scope until expiry).
RTT_REFRESH_TOKEN=
